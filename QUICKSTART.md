# 🚀 راهنمای سریع اجرا

## مرحله ۱: نصب وابستگی‌ها

```powershell
# فعال‌سازی محیط مجازی
.venv\Scripts\activate

# نصب کتابخانه‌ها
pip install -r requirements.txt
```

## مرحله ۲: اجرای تحلیل کامل

```powershell
# اجرای اسکریپت اصلی
python run_analysis.py
```

یا:

```powershell
cd src
python main.py
```

## مرحله ۳: مشاهده گزارش‌ها

بعد از اجرای موفق برنامه:

1. **گزارش‌های HTML**: در پوشه `reports/` قرار دارند
   - فایل‌های `report_*.html` را با مرورگر باز کنید
   
2. **گزارش‌های PDF**: در پوشه `reports/` قرار دارند
   - فایل‌های `report_*.pdf` برای پرینت A4 آماده هستند
   
3. **گزارش‌های Excel**: در پوشه `reports/` قرار دارند
   - فایل‌های `allocation_*.xlsx` شامل تحلیل تخصیص منابع

## ویژگی‌های گزارش‌ها

✅ تمام متن‌ها به فارسی
✅ تمام اعداد به فارسی
✅ طراحی زیبا با CSS
✅ سایز A4 استاندارد
✅ آماده برای پرینت
✅ شامل نمودارها و جداول
✅ قابل تبدیل به PDF

## نکات مهم

- **اتصال اینترنت**: برای دریافت داده‌های قیمتی لازم است
- **داده‌های صورت مالی**: فایل‌های MHTML باید در پوشه `Data/` موجود باشند
- **قیمت‌های تعدیل شده**: سیستم به طور خودکار از قیمت‌های تعدیل شده استفاده می‌کند
- **زمان اجرا**: بسته به سرعت اینترنت، اجرای کامل ۵-۱۵ دقیقه طول می‌کشد

## تست سریع

برای تست فقط بخش قیمتی:

```powershell
python test_price_data.py
```

## مشکلات رایج

### خطای نصب WeasyPrint
اگر WeasyPrint نصب نشد:
```powershell
# در ویندوز، ابتدا GTK3 را نصب کنید:
# از سایت https://github.com/tschoonj/GTK-for-Windows-Runtime-Environment-Installer دانلود کنید
```

### خطای فونت فارسی
اگر فونت‌های فارسی به درستی نمایش داده نمی‌شوند:
- مطمئن شوید فونت Tahoma روی سیستم نصب است
- می‌توانید در `report_generator.py` فونت را تغییر دهید

## پشتیبانی

برای مشکلات:
1. بررسی فایل لاگ در پوشه `output/`
2. اجرای `test_price_data.py` برای تشخیص مشکل
3. بررسی اتصال اینترنت و دسترسی به داده‌های بورس

---

**آخرین به‌روزرسانی**: ۱۴۰۴/۰۸/۲۵
